{
  "name": "omni",
  "version": "1.0.0",
  "description": "a tool for doing tic-80 things",
  "main": "scripts/package.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/package.js",
    "start": "watch 'npm run build' ./src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "watch": "^1.0.2"
  }
}
